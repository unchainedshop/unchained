import { EventDirector } from './EventDirector';

const {
  emit,
  getEmitAdapter,
  getEmitHistoryAdapter,
  registerEvents,
  setEmitAdapter,
  setEmitHistoryAdapter,
} = EventDirector;

export {
  emit,
  getEmitAdapter,
  getEmitHistoryAdapter,
  registerEvents,
  setEmitAdapter,
  setEmitHistoryAdapter,
};
