const deps = window.__UNCHAINED_PLUGIN_DEPS__?.['@apollo/client'];
export const useQuery = deps?.useQuery;
export const useMutation = deps?.useMutation;
export const useApolloClient = deps?.useApolloClient;
export const gql = deps?.gql;
