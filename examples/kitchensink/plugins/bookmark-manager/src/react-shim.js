const React = window.__UNCHAINED_PLUGIN_DEPS__?.react;
export default React;
export const {
  useState,
  useEffect,
  useCallback,
  useRef,
  useMemo,
  useContext,
  createContext,
  createElement,
  Fragment,
  Component,
  forwardRef,
  memo,
  lazy,
  Suspense,
  Children,
  cloneElement,
  isValidElement,
} = React || {};
