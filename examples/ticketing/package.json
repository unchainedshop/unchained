{
  "name": "@unchainedshop/example-ticketing",
  "description": "Kitchensink for Unchained Engine",
  "private": true,
  "version": "5.0.0-alpha.0",
  "license": "EUPL-1.2",
  "type": "module",
  "homepage": "https://unchained.shop",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/unchainedshop/unchained.git"
  },
  "contributors": [
    "Vedran Rudelj <vedran@unchained.shop> (https://unchained.shop)",
    "Pascal Kaufmann <pascal@unchained.shop> (https://unchained.shop)",
    "Marco Wettstein <maw@panter.ch>",
    "Simon Emanuel Schmid <simon@unchained.shop> (https://unchained.shop)",
    "Mikael Araya Mengistu <mikaeln@unchained.shop> (https://unchained.shop)"
  ],
  "bugs": {
    "url": "https://github.com/unchainedshop/unchained/issues"
  },
  "engineStrict": true,
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "lint": "prettier -w .",
    "clean": "tsc -b --clean",
    "build": "tsc -b",
    "start": "node --env-file=.env.defaults --env-file-if-exists=.env lib/boot.js",
    "dev": "node --no-warnings --env-file=.env.defaults --env-file-if-exists=.env --watch boot.ts",
    "test:run:integration": "run-p -r test:integration:*",
    "test:integration:start": "node --no-warnings --env-file=.env.defaults --env-file ../../.env.tests --env-file-if-exists=../../.env --import ./load_env.js --watch boot.ts",
    "test:integration:run": "node --no-warnings --env-file ../../.env.tests --env-file-if-exists=.env --test-global-setup=../../tests/helpers.js --test --test-concurrency=1 tests/*.test.*"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/multipart": "^9.0.3",
    "@unchainedshop/platform": "^5.0.0-alpha.0",
    "@unchainedshop/plugins": "^5.0.0-alpha.0",
    "@unchainedshop/ticketing": "^5.0.0-alpha.0",
    "fastify": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3"
  }
}
